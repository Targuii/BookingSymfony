        <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-3">
            <a class="navbar-brand" href="{{ path('home')}}"><i class="fas fa-user-cog"></i></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor01">
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="{{path('admin_dashboard')}}"><i class="fas fa-chart-line"></i> Tabeau de bord</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{path('admin_ads_list')}}"><i class="fas fa-hotel"></i> Annonces</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{path('admin_bookings_list')}}"><i class="fas fa-folder"></i> Réservation</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{path('admin_comments_list')}}"><i class="fas fa-comments"></i> Commentaires</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{path('admin_users_list')}}"><i class="fa fa-users"></i> Utilisateurs</a>
                        </li>
                    </ul>
                    <ul class="navbar-nav ml-auto">
                        <li class="nav-item dropdown">
                            {% if app.user %}
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <img src="{{app.user.avatar}}" alt="avatar de {{app.user.fullName}}" class="avatar avatar-sm">
                            {{app.user.fullName}}
                            </a>
                            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                            {# <a class="dropdown-item" href="{{path('account_home')}}"><i class="fas fa-user-circle"></i> Mon compte</a> #}
                            <a class="dropdown-item" href="{{path('home')}}" target="_blank"><i class="fas fa-eye"></i> Voir le site</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{path('admin_account_logout')}}"><i class="fas fa-plug"></i> Déconnexion</a>
                            </div>
                            {% else %}
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Mon Compte
                            </a>
                            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{path('account_register')}}">Inscription</a>
                            <a class="dropdown-item" href="{{path('account_login')}}">Connexion</a>
                            </div>
                            {% endif %}
                        </li>
                    </ul>
            </div>
        </nav>