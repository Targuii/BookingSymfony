{% extends 'base.html.twig' %}

{% block title %}Changement de Mot de Passe{% endblock %}


{% block body %}

<div class="container mt-5">
    <div class="row">
        <div class="offset-md-3 col-md-6">
        <h4 class="ml-2"><i class="fas fa-key"></i> Modifiez votre Mot de Passe.</h4>
            <div class="jumbotron py3 px-3">
                {{ form_start(form) }}

                {{ form_widget(form) }}

                <button type="submit" class="btn btn-info">Enregistrer</button>
                <a href="{{path('account_home')}}" class="btn btn-primary ml-3"><i class="fas fa-undo"></i> Retour</a>

                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>

{% endblock %}